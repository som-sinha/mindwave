name: mindwave
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  uuid:
  provider:
  firebase_core: ^3.13.0
  firebase_auth:
  cloud_firestore: 
  firebase_storage:
  image_cropper:
  image_picker:
  google_sign_in:
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/